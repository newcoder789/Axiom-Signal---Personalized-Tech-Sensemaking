# Axiom Architecture (v0)

User input → Agentic reasoning workflow → Decision output

The system uses:
- LangGraph for structured agent workflows
- Opik for observability and evaluation
- STM/LTM for personalization

Initial focus is a single decision flow.
